import { combineReducers } from 'redux'

const masterReducer = combineReducers({

})
// will combine addEvent, addSeries, setEventListFilter, and toggleVisibleEvent reducers

export default masterReducer
